{
  "name": "@coffeemakerstudio/roast",
  "version": "0.2.0",
  "description": "Coffee Maker Studio generic engine kernel, contracts, SDK, deterministic utilities, audio, and presentation infrastructure.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "bun run clean && tsc -p tsconfig.json && bun build src/index.ts --outdir dist --format esm",
    "test": "bun test tests/*.test.ts",
    "clean": "rm -rf dist"
  }
}
